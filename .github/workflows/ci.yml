name: CI

on:
  push:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  changes:
    name: Detect what changed
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust || github.event_name == 'workflow_dispatch' }}
      java: ${{ steps.filter.outputs.java || github.event_name == 'workflow_dispatch' }}
      python: ${{ steps.filter.outputs.python || github.event_name == 'workflow_dispatch' }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v7.0.1

      - name: detect changed languages
        id: filter
        if: github.event_name != 'workflow_dispatch'
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d
        with:
          filters: |
            shared: &shared
              - '.github/workflows/**'
              - '.github/actions/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '.cargo/config.toml'
              - 'deny.toml'
              - 'proto/**'
              - 'bindings/**'
            rust:
              - *shared
              - 'core/**'
              - 'client/**'
              - 'bench/**'
              - 'xtask/**'
            java:
              - *shared
              - 'core/**'
              - 'client/**'
            python:
              - *shared
              - 'core/**'
              - 'client/**'
              - 'bench/python/**'

  fmt:
    name: Check formatting
    needs: ["changes"]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v7.0.1

      - name: install rust
        uses: dtolnay/rust-toolchain@02cb101ec7c40f2c49e1d9714d64511d8e1b74de
        with:
          toolchain: stable
          components: rustfmt

      - name: check formatting
        run: cargo fmt --all -- --check

  deny:
    name: Audit dependencies and licenses
    needs: ["changes"]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v7.0.1

      - name: audit with cargo-deny
        uses: EmbarkStudios/cargo-deny-action@v2.1.1

  cpp:
    name: C++ Client - ${{ matrix.name }}
    needs: ["changes"]
    if: needs.changes.outputs.rust == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64
            runner-os: ubuntu-24.04
            platform: linux-x86_64
          - name: Linux aarch64
            runner-os: ubuntu-22.04-arm
            platform: linux-aarch64
          - name: macOS aarch64
            runner-os: macos-15
            platform: darwin-arm64
          - name: Windows x86_64
            runner-os: windows-2022
            platform: windows-x86_64
    runs-on: ${{ matrix.runner-os }}
    env:
      WPILIB_VERSION: 2027.0.0-alpha-7
    steps:
      - name: checkout repository
        uses: actions/checkout@v7.0.1

      - name: install rust
        uses: ./.github/actions/rust
        with:
          cache-key: cpp-${{ matrix.platform }}

      - name: build c abi and header
        run: cargo build -p tarwyn-c --release --locked

      - name: check committed header matches abi
        run: git diff --exit-code -- bindings/c/include/tarwyn.h

      - name: fetch wpimath headers
        run: |
          mkdir -p wpilib/include
          curl -sSL -o wpimath.zip \
            "https://frcmaven.wpi.edu/artifactory/release/org/wpilib/wpimath/wpimath-cpp/${WPILIB_VERSION}/wpimath-cpp-${WPILIB_VERSION}-headers.zip"
          curl -sSL -o wpiutil.zip \
            "https://frcmaven.wpi.edu/artifactory/release/org/wpilib/wpiutil/wpiutil-cpp/${WPILIB_VERSION}/wpiutil-cpp-${WPILIB_VERSION}-headers.zip"
          unzip -qo wpimath.zip -d wpilib/include
          unzip -qo wpiutil.zip -d wpilib/include

      - name: build c++ client
        run: |
          cmake -S bindings/cpp -B build/cpp -DWPILIB_INCLUDE_DIR="${{ github.workspace }}/wpilib/include" -DTARWYN_C_TESTS=OFF
          cmake --build build/cpp

      - name: run tests
        run: |
          case "${{ runner.os }}" in
            Linux) export LD_LIBRARY_PATH="${{ github.workspace }}/target/release" ;;
            macOS) export DYLD_LIBRARY_PATH="${{ github.workspace }}/target/release" ;;
            Windows) export PATH="${{ github.workspace }}/target/release:$PATH" ;;
          esac
          ctest --test-dir build/cpp --output-on-failure

  rust:
    needs: ["changes"]
    if: needs.changes.outputs.rust == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64
            runner-os: ubuntu-22.04
            cargo-target: x86_64-unknown-linux-gnu

          - name: Linux aarch64
            runner-os: ubuntu-22.04-arm
            cargo-target: aarch64-unknown-linux-gnu

          - name: macOS aarch64
            runner-os: macos-15
            cargo-target: aarch64-apple-darwin

          - name: Windows x86_64
            runner-os: windows-2022
            cargo-target: x86_64-pc-windows-msvc

    name: Rust - ${{ matrix.name }}
    runs-on: ${{ matrix.runner-os }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v7.0.1

      - name: install rust
        uses: ./.github/actions/rust
        with:
          targets: ${{ matrix.cargo-target }}
          components: clippy
          cache-key: ${{ matrix.cargo-target }}

      - name: build workspace
        run: |
          cargo build \
            --workspace \
            --locked --all-features \
            --target ${{ matrix.cargo-target }}

      - name: lint workspace
        run: |
          cargo clippy \
            --workspace \
            --locked --all-features \
            --target ${{ matrix.cargo-target }} \
            -- -D warnings

      - name: run tests
        run: |
          cargo test \
            --workspace \
            --locked --all-features --no-fail-fast \
            --target ${{ matrix.cargo-target }}

      - name: build docs
        if: matrix.cargo-target == 'x86_64-unknown-linux-gnu'
        env:
          RUSTDOCFLAGS: -D warnings
        run: |
          cargo doc \
            --workspace --no-deps \
            --locked --all-features \
            --target ${{ matrix.cargo-target }}

  natives:
    name: Server Binary - ${{ matrix.name }}
    needs: ["changes"]
    if: needs.changes.outputs.python == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64
            runner-os: ubuntu-22.04
            cargo-target: x86_64-unknown-linux-gnu
            platform: linux-x86_64
            server-file: tarwyn_server

          - name: Linux aarch64
            runner-os: ubuntu-22.04-arm
            cargo-target: aarch64-unknown-linux-gnu
            platform: linux-aarch64
            server-file: tarwyn_server

          - name: macOS aarch64
            runner-os: macos-15
            cargo-target: aarch64-apple-darwin
            platform: darwin-arm64
            server-file: tarwyn_server

          - name: Windows x86_64
            runner-os: windows-2022
            cargo-target: x86_64-pc-windows-msvc
            platform: windows-x86_64
            server-file: tarwyn_server.exe

    runs-on: ${{ matrix.runner-os }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v7.0.1

      - name: install rust
        uses: ./.github/actions/rust
        with:
          targets: ${{ matrix.cargo-target }}
          cache-key: natives-${{ matrix.cargo-target }}

      - name: build server
        run: cargo build -p tarwyn_server --release --target ${{ matrix.cargo-target }}

      - name: upload server
        uses: actions/upload-artifact@v7.0.1
        with:
          name: server-${{ matrix.platform }}
          path: target/${{ matrix.cargo-target }}/release/${{ matrix.server-file }}
          if-no-files-found: error

  java:
    name: Java Client - ${{ matrix.name }}
    needs: ["changes"]
    if: needs.changes.outputs.java == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64
            runner-os: ubuntu-22.04
            platform: linux-x86_64
          - name: Linux aarch64
            runner-os: ubuntu-22.04-arm
            platform: linux-aarch64
          - name: macOS aarch64
            runner-os: macos-15
            platform: darwin-arm64
          - name: Windows x86_64
            runner-os: windows-2022
            platform: windows-x86_64
    runs-on: ${{ matrix.runner-os }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v7.0.1

      - name: install java
        uses: actions/setup-java@v6.0.1
        with:
          distribution: temurin
          java-version: "25"
          cache: gradle

      - name: install rust
        uses: ./.github/actions/rust
        with:
          cache-key: java-${{ matrix.platform }}

      - name: build java client
        working-directory: bindings/java
        run: ./gradlew build javadoc

  python:
    name: Python Client - ${{ matrix.name }}
    needs: ["changes", "natives"]
    if: needs.changes.outputs.python == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64
            runner-os: ubuntu-24.04
            platform: linux-x86_64
            server-file: tarwyn_server
          - name: Linux aarch64
            runner-os: ubuntu-24.04-arm
            platform: linux-aarch64
            server-file: tarwyn_server
          - name: macOS aarch64
            runner-os: macos-15
            platform: darwin-arm64
            server-file: tarwyn_server
          - name: Windows x86_64
            runner-os: windows-2022
            platform: windows-x86_64
            server-file: tarwyn_server.exe
    runs-on: ${{ matrix.runner-os }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v7.0.1

      - name: install python
        uses: astral-sh/setup-uv@v10.2.0
        with:
          python-version: "3.14"

      - name: install rust
        uses: ./.github/actions/rust
        with:
          cache-key: python-${{ matrix.platform }}

      - name: lint python
        if: matrix.platform == 'linux-x86_64'
        run: |
          uvx ruff@0.16.8 check bindings/python bench/python
          uvx ruff@0.16.8 format --check bindings/python bench/python

      - name: run tests
        working-directory: bindings/python
        run: uv run --group dev pytest tests --ignore=tests/protocol -q

      - name: download server
        uses: actions/download-artifact@v8.0.1
        with:
          name: server-${{ matrix.platform }}
          path: ./server

      - name: check nt conformance
        working-directory: bindings/python
        shell: bash
        env:
          TARWYN_SERVER_BIN: ${{ github.workspace }}/server/${{ matrix.server-file }}
        run: |
          chmod +x "$TARWYN_SERVER_BIN"
          uv run --group dev pytest tests/protocol -q
  conclusion:
    name: Conclusion
    needs: ["changes", "fmt", "deny", "cpp", "rust", "natives", "java", "python"]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: check job success
        env:
          needs: ${{ toJson(needs) }}
        run: |
          jq -C <<< "${needs}"
          passed=$(jq -r 'all(.result as $result | ["success", "skipped"] | contains([$result]))' <<< "${needs}")
          [ "$passed" = "true" ]
