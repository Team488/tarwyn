[package]
name = "tarwyn_client"
version = "0.1.0"
edition = "2024"
description = "A Rust client for interacting with the tarwyn server over ZeroMQ."
license = "MIT"

[lints]
workspace = true

[dependencies]
thiserror = "2.0"
tarwyn_protobuf = { path = "../proto", version = "0.1.0" }
tarwyn_server = { path = "../core" }
prost = "0.14.4"
slotmap = "1.1"
tungstenite = "0.30"
serde_json = "1"
